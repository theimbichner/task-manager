version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    docker:
      - image: circleci/python:3.8
    executor: python/default
    steps:
      - checkout
      - python/install-deps
      - run:
          command: |
            coverage run --branch -m pytest -v
            coverage report
            coverage html
            coverage xml -o testresults/results.xml
          name: Test
      - store_artifacts:
          path: htmlcov
      - store_test_results:
          path: testresults

workflows:
  main:
    jobs:
      - build-and-test
